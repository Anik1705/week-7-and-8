Consider this function:

def mystery(x, y) :
	result = (x + y) / (y - x)
	return result
What is the result of the call mystery(2, 3)?

 # it is 5.0
 code :
 def mystery(x, y) :
	result = (x + y) / (y - x)
	return result
  
 def main () :
     print ("The result of call mystery (2, 3)" ,mystery(2, 3))
 main ()
 --------------------------------------------------------------------------------------
What does this program print? 

 def main() :
	a = 5
	b = 7
	print(mystery(a, b))
def mystery(x, y) :
	z = x + y
	z = z / 2.0
	return z
  
# it is  6.0
----------------------------------------------------------------------------------------
What does this program print? 

def main() :
a = 4
print(mystery(a + 1))
def mystery(x) :
y = x * x
return y
main()

# it is 25
-----------------------------------------------------------------------------------------
